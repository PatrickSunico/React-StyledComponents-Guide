import React from 'react';

function App() {
  return (
    <main>
      {/* {movies.map((movie) => (
        <Card classNames="mr" key={movie.id}>
          <Card.Image src={movie.image} alt={movie.title} />
        </Card>
      ))} */}
    </main>
  );
}

export default App;
